NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional for Enrichment admin page (status + run). If unset, the app loads
# scripts/scraper/.env when needed (so you can reuse the scraper env instead of duplicating).
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin Basic Auth (defaults: superadmin / superpass). Protects /admin and /api/admin.
# ADMIN_BASIC_USER=superadmin
# ADMIN_BASIC_PASSWORD=superpass
